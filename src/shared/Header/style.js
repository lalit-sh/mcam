import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    header: {
        backgroundColor: "#fff",
        color: "#000"
    },
    text: {
        color: "#000"
    }

});