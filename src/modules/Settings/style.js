import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    logout: {
        textAlign: "center",
        fontSize: 24,
        color: "#f44336",
        fontWeight: "bold"
    }
});